// Moved to @/components/ui/Select so non-helpdesk modules can use it without
// importing across a module boundary. Re-exported here so existing helpdesk
// imports keep working.
export { default } from '@/components/ui/Select'
